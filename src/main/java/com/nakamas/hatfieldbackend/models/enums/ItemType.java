package com.nakamas.hatfieldbackend.models.enums;

//todo: to be changed in the future into a table with a setting
// that specifies the showed columns for each type
public enum
ItemType {
    DEVICE, PART, ACCESSORIES, CABLES, OTHER
}
